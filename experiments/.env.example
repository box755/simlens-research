# 複製這個檔案成 .env（已 gitignored），填入實際值。
# ⚠️ 千萬不要把真實 API key 填進這個 .env.example 檔！
# .env.example 會被 commit 到 git；.env 才是 gitignored 的本機機密檔。
#
# v4.1 預算決策：所有 OpenAI 呼叫一律使用 GPT-4o-mini
# 預算總計（含 Week 6 之前所有 API）：~$28.6 USD
# 詳細拆解見 SimLens_Research_Plan_v4.1.md §8.2

# Anthropic — Claude 蒸餾 + persona activity + Claude zero-shot baseline
# 用量：1,648 calls，預估 ~$24.6
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI — text-embedding-3-small + GPT-4o-mini（一律 mini，預算決策）
# 用量：~$4
OPENAI_API_KEY=sk-...

# HuggingFace token — 下載 Llama-3.2-3B-Instruct（gated model）+ LLaVA-NeXT
# 必須先去 https://huggingface.co/meta-llama/Llama-3.2-3B-Instruct 同意 license
# 費用：$0
HF_TOKEN=hf_...

# YouTube Data API v3 — 收集 100 部影片 metadata
# 費用：$0（10,000 unit/day quota 內）
YOUTUBE_API_KEY=AIza...

# Optional: Weights & Biases 追蹤訓練
WANDB_API_KEY=
WANDB_PROJECT=simlens-research

# Ollama endpoint — Qwen3-32B-Q4 reward judge
OLLAMA_HOST=http://localhost:11434
